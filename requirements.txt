# AlphaScan — Minimal Dependencies
# Only what's needed: scanning, parsing, validating, reporting.

requests==2.31.0
python-dotenv==1.0.0
mistralai==1.0.0

# Optional: for Ethereum address derivation from private keys
# eth-account==0.11.0
colorlog