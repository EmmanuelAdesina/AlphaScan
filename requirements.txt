# AlphaScan — Dependencies
# Core scanning, parsing, validating, reporting, and API.

requests==2.31.0
python-dotenv==1.0.0
mistralai==1.0.0
colorlog
fastapi
uvicorn

# Export API dependencies
starlette>=0.27.0

# Optional: for Ethereum address derivation from private keys
# eth-account==0.11.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
